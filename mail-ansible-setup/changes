under Internet host and domain names 

changed 
#myhostname = xxxxxxxx
to 
myhostname = mail.example48.lab

changed 
#mydomain
mydomain = example48.lab

!! under sending mail 

#myorgin = $mydomain
to
myorigin = $mydomain

!! under recived mail

#inet_interfaces = all 
to
inet_interfaces = all

inet_interfaces = localhost
to 
#inet_interfaces = localhost

added 
mydestination = $myhostname, localhost.$mydomain, localhost, $mydomain, mail.$mydomain
where the other my destinations were


mydestination = $myhostname, localhost.$mydomain, localhost
to
#mydestination = $myhostname, localhost.$mydomain, localhost

smtpd_banner = $myhostname ESMTP $mail_name
mynetworks = 127.0.0.0/8 172.16.30.0/24 172.16.31.0/24

aliased geeks to cst8246
vim /etc/aliases 
added 
geeks: cst8246
at the bot 
then 
ran 
newaliases
postaliases /etc/aliases




sudo dnf install -y httpd php php-mbstring php-common php-curl php-json unzip
sudo systemctl enable --now httpd


cd /var/www/html
sudo curl -sL https://www.rainloop.net/repository/webmail/rainloop-community-latest.zip -o rainloop.zip
sudo unzip rainloop.zip -d rainloop
sudo chown -R apache:apache rainloop
sudo restorecon -R rainloop   # For SELinux




